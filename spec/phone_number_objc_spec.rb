require "spec_helper"

describe PhoneNumberObjc do
  it "has a version number" do
    expect(PhoneNumberObjc::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
